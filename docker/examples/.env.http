# Environment Variables for nrest.nvim Demo
#
# This file contains variables that can be used across all .http files
# in this directory when env_file = 'auto' is configured
#
# ⚠️  In production, add .env.http to .gitignore to avoid committing secrets!

# Base URLs
@baseUrl = https://httpbin.org
@apiUrl = https://api.example.com
@devUrl = https://dev.api.example.com
@prodUrl = https://prod.api.example.com

# Authentication tokens (demo values - use real tokens in production)
@demoToken = demo-bearer-token-12345
@apiKey = sk-demo-api-key-abcdef
@basicAuthUser = demo-user
@basicAuthPass = demo-password

# API versioning
@apiVersion = v1
@apiVersionHeader = application/vnd.api.v1+json

# Common headers
@userAgent = nrest.nvim/0.1.0
@contentType = application/json
@acceptType = application/json

# Request IDs and tracking
@clientId = nrest-demo-client
@requestIdPrefix = nrest-req

# Timeouts and limits
@defaultTimeout = 10000
@maxRetries = 3

# Feature flags
@enableDebug = true
@enableLogging = false

# User information (can reference system vars)
@currentUser = $USER
@homeDir = ${HOME}

# Notes:
# - Variables defined here can be used with {{variableName}}
# - System environment variables use $VAR or ${VAR}
# - Buffer variables override these .env.http variables
# - Great for sharing config across multiple .http files
